# Data Notice

The public repository does not include the original experiment tables or
pretrained model weights.

The preprocessing code expects curated data directories such as:

```text
experiments/
direction_text_csvs_en_v3/
downloaded_structures/
```

